{
  "name": "duster",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/sidecar",
    "packages/provisioner",
    "packages/workflows/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "vitest run",
    "test:integration": "vitest run tests/integration",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "yaml": "^2.9.0"
  }
}
